// Placeholder home — replaced by the real tile-grid home in slice 2 (auth + home).
export default function HomePage() {
  return (
    <main className="mx-auto max-w-2xl p-8">
      <h1 className="text-2xl font-semibold">MockSocial</h1>
      <p className="mt-2 text-gray-600">
        Faithful TypeScript port in progress. Platforms come online slice by
        slice: MockTweet, MockBook, MockGram, MockTube.
      </p>
    </main>
  );
}
